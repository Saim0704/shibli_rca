export const NODE_ENV = process.env.NODE_ENV || "development";

export const SERVER_ROOT_URL = "http://localhost:5000";

export const token = window.localStorage.getItem("jwtToken");

export const constants = {
  appThemeColor: "#3b9ad7",
  dangerColor: "#ff4d4f",
  infoColor: "#1890ff",
  successColor: "#52c41a",
};
